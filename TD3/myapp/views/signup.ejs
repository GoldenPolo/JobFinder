<!DOCTYPE html> 
<html lang="fr">
    <head>
    <meta charset="UTF-8"> 
    <title></title>
    <meta name="description" content="">
    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    </head> 
    <body> 
        <!-- Latest compiled JavaScript -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>

        <div class="container-fluid d-flex justify-content-center align-items-center vh-100">
            <div class="col col-4 border">
                <div class="row text-center mt-3">
                    <h2 class="">Inscription</h2>
                </div>
                <div class="row m-3 mx-5 text-center justify-content-center align-items-center">
                    <form action="/signup" method="post">
                        <input type="text" class="form-control mb-3" id="mail" placeholder="Email" name="mail">
                        <input type="text" class="form-control mb-3" id="prenom" placeholder="Prénom" name="prenom">
                        <input type="text" class="form-control mb-3" id="nom" placeholder="Nom" name="nom">
                        <input type="text" class="form-control mb-3" id="tel" placeholder="Téléphone" name="tel">
                        <input type="password" class="form-control mb-3" id="pwd" placeholder="Mot de passe" name="pwd"> 
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary">Créer mon compte</button>
                        </div>
                    </form> 
                    <% if (message) { %>
                        <div class="alert alert-danger mt-3" role="alert">
                            <%= message %>
                        </div>
                    <% } %>
                </div>
                <div class="row text-center mb-3">
                    <a class="btn" href="/signup">Déjà un compte ? Connexion</a>
                </div>
            </div>
        </div>
    </body> 
</html>
