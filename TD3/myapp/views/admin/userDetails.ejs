<!DOCTYPE html> 
<html lang="fr">
    <head>
    <meta charset="UTF-8"> 
    <title></title>
    <meta name="description" content="">
    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    </head> 
    <body> 
        <!-- Latest compiled JavaScript -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>

        <nav class="navbar navbar-expand-sm">
            <div class="container-fluid d-flex border-top-0 border-bottom border-dark">
                <div class="col col-2">
                    <img class="img-fluid m-2" src="/images/logo.png" alt="logo">
                </div>
                <ul class="navbar-nav">
                    <li>
                        <a class="btn btn-outline-secondary m-2 me-5" href="/admin/usersList">Utilisateurs</a>
                    </li>
                    <li>
                        <a class="btn btn-outline-secondary m-2 me-5" href="/admin/organisationsList">Organisations</a>
                    </li>
                    <li>
                        <a class="btn btn-outline-secondary m-2 me-5" href="/logout">Déconnexion</a>
                    </li>
                </ul>
            </div>
        </nav>

        <div class="card m-5">
            <div class="d-flex flex-row justify-content-center">
                <div class="col col-3 m-3">
                    <div class="row">
                        <p>Id : <%= user.id %></p><br> <!-- enlever a la ligne ? -->
                    </div>
                    <div class="row">
                        <p>Nom : <%= user.nom %></p>
                    </div>
                    <div class="row">
                        <p>Date de création : <%= moment(user.dateCreation).format('L') %></p>
                    </div>
                </div>
                <div class="col col-3 m-3">
                    <div class="row">
                        <p>Email : <%= user.email %></p><br> <!-- enlever a la ligne ? -->
                    </div>
                    <div class="row">
                        <p>Prénom : <%= user.prenom %></p>
                    </div>
                    <div class="row">
                        <p>Id Organisation : <%= user.organisation %></p>
                    </div>
                </div>
                <div class="col col-3 m-3">
                    <div class="row">
                        <p>Type : <%= user.type %></p><br> <!-- enlever a la ligne ? -->
                    </div>
                    <div class="row">
                        <p>Téléphone : <%= user.telephone %></p>
                    </div>
                    <div class="row">
                        <p>Est Actif : <%= user.estActif %></p>
                    </div>
                </div>
            </div>
        </div>

        <div class="row justify-content-center">
            <div class="col col-1">
                <a class="btn btn-outline-primary" href="/admin/userAdmin?id=<%= user.id %>">Admin</a>
            </div>
            <div class="col col-1">
                <a class="btn btn-outline-primary" href="/admin/userModif?id=<%= user.id %>">Modifier</a>
            </div>
            <div class="col col-1">
                <a class="btn btn-outline-primary" href="/admin/userDelete?id=<%= user.id %>">Supprimer</a>
            </div>
        </div>
        
    </body> 
</html>
